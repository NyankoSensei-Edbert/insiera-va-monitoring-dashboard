services:
  monitoring:
    build: .
    ports:
      - "5050:5050"
    environment:
      # Point this at your real INSIERA-VA backend
      BACKEND_URL: ${BACKEND_URL}
      VERIFY_SSL: ${VERIFY_SSL}
      PORT: ${PORT}
    restart: unless-stopped
